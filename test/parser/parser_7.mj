// modified grammar test
program changes
    int arr[];
{
    void main()
        int x, i;
    {
        arr = new int[2];
        arr[0] = 5;
        arr[1] = 2;
        x = 0;

        x += 2;
        arr[0] *= x -= 1;

        foreach (i : arr) print(i);
    }
}